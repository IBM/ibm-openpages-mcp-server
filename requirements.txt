# Dependencies for LOCAL MCP Server (stdio mode)
# This file contains only the minimal dependencies needed for local stdio mode
# For remote HTTP mode, additional dependencies (FastAPI, Uvicorn, etc.) would be needed

# MCP Protocol
mcp>=1.9.4

# HTTP Client (for OpenPages API calls)
httpx>=0.24.0

# Configuration & Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Date/Time Utilities
python-dateutil>=2.8.2

# JWT token handling (if needed for OpenPages auth)
PyJWT>=2.8.0
